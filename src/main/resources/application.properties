# Server
server.port=8080
server.servlet.context-path=/api
spring.application.name=RoadWarningsNarino

# Database (Render / Supabase)
spring.datasource.url=jdbc:postgresql://db.jdhicdojibbdlxiheuka.supabase.co:5432/postgres
spring.datasource.username=${RoadWarningsNarino}
spring.datasource.password=${U6_D@y+TGcQW5ZR}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Security
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.packages-to-scan=com.roadwarnings.narino.controller

# Logging
logging.level.root=INFO
logging.level.com.roadwarnings.narino=DEBUG

# Deshabilitar seguridad temporalmente
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
